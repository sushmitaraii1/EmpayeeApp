@model IEnumerable<EmpayeeApp.Models.TaxBrackekt>

@{
    ViewBag.Title = "BracketDetails";
}

<h2>BracketDetails</h2>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.First_Bracket)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Second_Bracket)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Third_Bracket)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fourth_Bracket)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fifth_Bracket)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.First_Bracket)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Second_Bracket)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Third_Bracket)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fourth_Bracket)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fifth_Bracket)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Bracket_Id }) |

            </td>
        </tr>
    }

</table>
